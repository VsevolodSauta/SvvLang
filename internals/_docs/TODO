Что сделать:
Первая контрольная точка:
    Поддержание порядка:
    - разделить и переименовать внутренний объект "карта" в "отображение" и "инъекция".
    - разделить каждый объект в свои файлы(один файл описывает не более одного объекта).

    Реализации:
    - реализовать внутренний объект "множество строк".
    - реализовать внутренние объекты "отображение строк".

    Тесты:
    - список.
    - отображение.
    - инъекция.
    - множество строк.

Вторая контрольная точка:
    Поддержание порядка:
    - переход на CMake

    Реализации:
//    - реализация битового массива
//    - реализация битового потока
//    - реализация консольного ввода/вывода.


На когда-либо:
    Реализция:
      - реализовать "бинарное поисковое дерево", и на него портировать "множество", "отображение", "инъекцию".
      - реализовать инъекцию на одном дереве.
      - реализовать строки на массиве.
      - реализовать "инъекцию строк".
      - клонирование, итерирование стека и очереди.
      
    Оптимизация:
     - список: создать дополнительный код в "итераторе", переписать список с использованием всего функционала итератора.
     - переписать код сортировки списка для использования двух итераторов.
     - переписать код списка для использования "системного" итератора.
     - реализовать "стек" и "очередь" без использования "списка", а на "распределителе памяти".

    Тесты:
    - отображение строк.
    - строка.
